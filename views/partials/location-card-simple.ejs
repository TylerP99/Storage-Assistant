<% for(let i = 0; i < locations.length; ++i) { %>

    <section class="location-card-simple">

        <h2><%=locations[i].name%></h2>

        <p><%=locations[i].description%></p>

        <a href="/location/<%=locations[i]._id%>">View Location</a>

    </section>

<% } %>