<% for(let i = 0; i < items.length; ++i) { %>

    <section class="item-card-simple">

        <h4><%= items[i].name %></h4>

        <p><%= items[i].description %></p>

        <a class="view-button" href="/item/<%=items[i]._id%>">View Item</a>

    </section>

<% } %>